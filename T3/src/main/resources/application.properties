spring.application.name=aula-dbe
server.port=${SERVER_PORT:8080}
server.error.include-message=always

spring.datasource.url=${DB_URL:jdbc:postgresql://127.0.0.1:5432/trabalho3}
spring.datasource.username=${DB_USERNAME:postgres}
spring.datasource.password=${DB_PASSWORD:masterkey}

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.open-in-view=false

# Caminho do JSON OpenAPI gerado automaticamente.
springdoc.api-docs.path=/v3/api-docs
# Pacotes considerados na geracao da documentacao.
springdoc.packages-to-scan=org.api, org.config
# Caminho da interface Swagger UI.
springdoc.swagger-ui.path=/swagger-ui.html
# Ordena os endpoints por metodo HTTP na interface.
springdoc.swagger-ui.operationsSorter=method
# Ordena as tags em ordem alfabetica.
springdoc.swagger-ui.tagsSorter=alpha


